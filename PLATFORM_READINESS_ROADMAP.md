# 🚀 خارطة طريق: جاهزية منصّة JAOLA OS للعمل التجاري

> الهدف: ما الذي يفصل المنصّة عن «جاهزة للإطلاق والدخل». مرتّبة بالأولوية،
> ومبنيّة على الحالة الفعلية للكود (لا قائمة عامّة).

---

## 🔴 المستوى 1 — حواجز الإطلاق والدخل

| الفكرة | لماذا حرجة | الحالة |
|---|---|---|
| **معرض القوالب في الواجهة** | 13 قالباً عاملاً مخفيّة في الـ backend — يجب أن يبدأ المستخدم منها بضغطة | ✅ مكتمل — معرض «ابدأ من قالب» يعمل، والآن مُبرَز أيضاً في قسم «المستشارون الأذكياء» بصفحة الهبوط العامة |
| **الفوترة والحصص** (Stripe/Paddle + طبقات Free/Pro/Business) | لا دخل بلا اشتراكات وحدود استخدام (نداءات ذكاء/مشاريع/نشر) | ✅ البنية الكاملة موجودة (انظر تفصيلها أدناه في المستوى 2) — *المتبقي فقط: ضبط مفاتيح Stripe الحقيقية (قرار المالك، ليس كوداً)* |
| **إتمام أتمتة نشر Full-Stack** | 3 خطوات يدوية (ريبو + ربط + Render + DB) = أكبر احتكاك | ✅ الجولة أ **مكتملة في الكود ومربوطة فعلياً** (`backend/services/deployAutomation.js`، انظر `DEPLOY_AUTOMATION_ROADMAP.md` المحدَّث) — *المتبقي فقط: `GITHUB_PLATFORM_TOKEN` و`RENDER_API_KEY` (قرار حساب، ليس كوداً)* |
| **قاعدة بيانات مُدارة (multi-tenant DB)** | لتشغيل مشاريع المستخدمين بلا إعداد يدوي | غير موجود فعلاً (الجولة ب في `DEPLOY_AUTOMATION_ROADMAP.md`) — هذه فجوة حقيقية، لكنها قرار مزوّد/تكلفة متكرّرة (MongoDB Atlas API؟ عنقود مشترك؟) يخصّ المالك قبل أي كود |
| **نطاقات مخصّصة** (custom domain) | موقع بلا نطاق العميل غير احترافي للبيع | ✅ مكتمل — `backend/services/customDomains.js` (تحقّق نطاق + تعليمات DNS + ربط/فصل + حصة الخطة `customDomainsMax`)، مربوط في `server.js` وله واجهة مستخدم |

## 🟠 المستوى 2 — الثقة والاحتفاظ

- ✅ **لوحة صحّة المشروع للمستخدم**: عرض نتيجة التحقّق السلوكي («يعمل / ناقص كذا») بدل إخفائها.
- ✅ **نسخ احتياطية + تصدير + سجلّ إصدارات (UI)**: زرّ «تنزيل مشروعي (zip)» + استرجاع نسخة سابقة — الأسرار لا تُصدَّر أبداً.
- ✅ **الصور الحقيقية + الشعار**: مودال «🎨 هوية موقعك» — رفع الشعار (يصير favicon فوراً) + «صور حقيقية بالذكاء» فوق عقد imageForge (يستبدل الفارغ/المولَّد فقط، صور المستخدم لا تُمسّ، بحصة الخطة: مجاني 6 / Pro ‏150 / مؤسسات ∞ — مقياس `aiImages`؛ التفعيل بـ `OPENAI_API_KEY`، وبدونها تبقى SVG الحتمية).
- ✅ **تحليلات لموقع المستخدم + صندوق النماذج**: زيارات + رسائل «تواصل معنا» تصل لداشبورد صاحب الموقع.
- 💳 **الفوترة والحصص** (مُرحَّلة من المستوى 1 بقرار المالك) — ✅ البنية: خطط + Stripe Checkout/Portal/Webhook + بوابة عدد المشاريع + حصص ذكاء البوت الشهرية. *المتبقي للتفعيل: ضبط `STRIPE_SECRET_KEY` و`STRIPE_PRICE_PRO/ENTERPRISE` في بيئة الخادم.*

## 🤖 مسار الوكلاء كمنتج (اعتُمد 2026-07-25)

> القرار: الوكلاء قابلة للتخصيص والبيع — نبيع الأوركسترا (التعليمات + المنطق
> الحتمي + الحصص) لا النموذج. ثلاث مراحل بترتيب الثمرة الأقرب:

1. ✅ **استوديو البوت للمستخدم** — تخصيص كامل من الداشبورد (اسم/رمز/ترحيب/FAQ/أزرار/ذكاء حيّ) + بيان إعداد يُقرأ ويُعدَّل. *(هذه هي الميزة التي تُربط بالفوترة كخطّة مدفوعة)*
2. ✅ **البوت كخدمة مدفوعة** — حصص رسائل الذكاء الحيّ الشهرية بالخطة (مجاني 30 / Pro ‏2000 / مؤسسات ∞)، تُفرض في نقطة الدردشة العامة (عند النفاد يرتدّ الودجت لقاعدته الداخلية بصمت)، وتظهر في صفحة الفوترة.
3. ✅ **سوق الوكلاء** —
   - المقتطف المُستضاف: `embed.js?id=…` يركّب البوت في أي موقع، الكود على خادمنا.
   - **صناعة الوكلاء للمستخدمين** («🧩 وكلائي»): شخصية + معرفة يملكها المستخدم، بسقف الخطة (مجاني 1 / Pro ‏3 / مؤسسات ∞ — `customAgentsMax`)، لكل وكيل كود تضمين لأي موقع ودردشة عامة `/api/agent-chat` بحواجز أمان ثابتة (لا اختلاق، لا كشف تعليمات، لا انتحال) وبحصة ذكاء البوت الشهرية نفسها. العزل: معرفة الوكيل نصوص مالكه فقط — لا وصول لملفات أو أسرار.

### 📣 وكلاء القنوات (سوشيال + بريد) — اعتُمد 2026-07-25
1. ✅ **الجولة ١ (بلا تكاملات)**: المساعد التسويقي — أسبوع منشورات سوشيال من محتوى الموقع الفعلي (ذكاء + ارتداد حتمي، صور SVG بهوية العلامة) + «اقترح رداً» لرسائل صندوق النماذج.
2. ✅ **الجولة ٢**: بريد فعلي عبر Resend (مفتاح `RESEND_API_KEY` + `MAIL_FROM` اختياري) — «📧 أرسل بالبريد» للردود من الداشبورد بحصة الخطة (مجاني 20 / Pro ‏500 / مؤسسات ∞) + إشعار بريدي صامت للمالك عند كل رسالة واردة (سقف ثابت 300/شهر). *المتبقي من الجولة: النشرة البريدية لمشتركي الموقع.*
3. ✅ **الجولة ٣**: النشر المباشر والجدولة —
   - **تيليجرام**: توكن BotFather + قناة (تحقق getMe).
   - **فيسبوك (صفحة Meta)**: Page ID + توكن صفحة (تحقق حيّ لاسم الصفحة).
   - **X**: مفاتيح المطوّر الأربعة للمستخدم بتوقيع OAuth 1.0a (بلا اعتماديات).
   - **واتساب**: زر مشاركة (واجهتها العامة لا تدعم النشر لقناة — بصدق).
   - كل التوكنات مشفّرة (secretVault) ولا تصل الواجهة؛ زر «🚀 انشر» يبثّ لكل القنوات المربوطة، وكل إرسال يُحسب من حصة `socialPosts`.
   - **📅 الجدولة الآلية**: «جدول الأسبوع كاملاً» (يومياً ١٠ صباحاً) — مجدول ملفّي صامد + حلقة خادم كل دقيقة تلتقط المستحق وتنشر وتعلّم النتيجة، بسقف ٥٠ معلّقاً وسجلّ منفَّذ.

## 🟢 المستوى 3 — التوسّع والنموّ

- **جولا-بوت كمنتج مستقل** (مقتطف مُستضاف + multi-tenant) — `JAOLA_BOT_PRODUCT_ROADMAP.md`.
- **حسابات فرق/مساحات عمل** (مشاركة مشروع، أدوار).
- **White-label للوكالات** (بنيناه في القوالب والبوت — نعمّمه كخطّة بيع).
- **تكاملات**: مدفوعات المتاجر (Stripe Checkout)، واتساب/بريد للإشعارات، Google Analytics.

## ⚙️ جاهزية التشغيل (خلف الكواليس)

- **CI/CD**: تشغيل الاختبارات (250+) آلياً على كل PR (GitHub Actions) قبل الدمج.
- **مراقبة أخطاء الإنتاج** (Sentry) + سجلّات + تنبيهات.
- **بيئة staging** منفصلة عن الإنتاج.
- **حماية إساءة الذكاء** (حدود، كشف محتوى ضارّ) + ToS/الخصوصية/حذف البيانات.

---

## الترتيب الموصى به للتنفيذ
1. **معرض القوالب** (سريع، يُظهر القيمة فوراً) ← *بدأنا به*.
2. **الفوترة والحصص** (تشغيل الدخل).
3. **إتمام أتمتة النشر + DB مُدارة** (إزالة أكبر احتكاك).
4. ثم المستوى 2 (الثقة/الاحتفاظ) بالتوازي مع النموّ.

---

_سُجّلت بناءً على طلب المستخدم كمرجع للتطوير نحو الجاهزية التجارية._
