# 🧭 JAOLA OS — بريفنغُ الجلسة

> يُحمَّل تلقائيّاً في كلّ جلسة. **قصيرٌ عمداً**: كلُّ سطرٍ هنا يُدفَع ثمنُه في كلّ مرّة.
> وهو **يدلّ ولا يشرح** — الشرحُ في الملفّات المذكورة أسفلَه، والحقيقةُ في الشفرة والاختبارات.

## ما هذا

منصّةٌ تبني للمستخدم موقعاً/تطبيقاً كاملاً من وصفٍ بلغته. خمسُ خدمات:

| | |
|---|---|
| `backend/` | **قلبُ جولا** — المولّد، الوكلاء، بوّابات الحكم. أكثرُ العمل هنا |
| `frontend/` | واجهةُ المنصّة |
| `travel-service/` | جترافا (سفر) — له `CLAUDE.md` خاصّ |
| `logo-service/` · `video-service/` | خدمتان مستقلّتان |
| `workspace/` | مساحاتُ عمل المستخدمين المولَّدة (ليست شفرةَ المنصّة) |

## التحقّق — قبل أيّ دعوى «تمّ»

```bash
cd backend && node --test 'tests/*.test.mjs'      # ٢٠١٢ اختباراً في ٢٦٣ ملفّاً
```

ليس Jest. `node --test` وحدَه. والحزمةُ كلُّها تُشغَّل قبل كلِّ دفعة — لا ملفٌّ واحد.

## طريقةُ العمل (هذه هي الخلاصة، لا تتجاوزها)

1. **قِس قبل أن تبني.** رقمٌ من تشغيلٍ حقيقيّ، لا استنتاجٌ من قراءة. وإن سقطت الفرضيّةُ بالقياس، **سجِّل السقوطَ ولا تبنِ** — النتيجةُ السلبيّة مكسبٌ لا فشل (انظر `tests/goalFidelityMeasure.test.mjs`، `tests/coderChainMeasure.test.mjs`).
2. **اختبارُ الطفرة على كلِّ تغيير.** غيِّر شرطاً، شغِّل الاختبارات: إن نجحت فالحارسُ غيرُ مغطّى.
3. **الطفرةُ الناجية لا تُهمَل.** إمّا اختبارٌ ناقصٌ فيُكتَب، أو شفرةٌ ميّتةٌ فتُحذَف — **بتعليقٍ يقول إنّها قِيست لا خُمِّنت**.
4. **حارسُ الحدود يُحدَّث ولا يُسكَت.** حين يقع تثبيتُ سطرٍ أو عدّ، صحِّح الرقمَ **واكتب فوقه لماذا تغيّر**.
5. **لا تجريد بلا مستهلكٍ حيّ.** (درسُ `agentOrchestrator.js` المحذوف.)
6. **مصدرٌ واحدٌ لكلّ قاعدة.** النسختان تفترقان عند أوّل إضافة — هذه العلّةُ تكرّرت في هذه الشجرة مراراً.
7. **القائمةُ المغلقة تقف مقامَ كلام صاحب المشروع** — وهي أمُّ الأعطاب هنا (PM/15، PM/22 وما بعدهما). حين تجد قائمةَ أسماءٍ تحكم على نصٍّ كتبه إنسان، فتِّش عمّا تُسقطه.
8. **ما لم يُصلَح يُقال ويُقاس.** «مقيسٌ ولم يُصلَح» موقفٌ مشروعٌ ومكتوب — لا يُخفى ولا يُدّعى إصلاحُه.

## ⚠️ فِخاخٌ وقعت فعلاً

- **`\b` لا يعمل مع الحروف العربيّة** (الراء ليست `\w`). `أدوار\b` لا يطابق أبداً. التحذيرُ مكتوبٌ في `agents/textNormalizer.js`. وقع هذا الفخُّ **ثلاثَ مرّات**.
- **استبدالُ عميلٍ في الاختبار على نسخةِ وحدةٍ ذاتِ استعلام** (`import('…?x=1')`) لا يصل النسخةَ التي يستوردها المُختبَر. `AI_PROVIDERS` تُقرأ عند التحميل → اقِس في **عمليّةٍ ابنة** (`tests/providerSelection.test.mjs`).
- **ملفُّ اختبارٍ يستورد وحدةً تطبع نصّاً غيرَ لاتينيّ** يُفسد قناةَ تقرير `node --test`. الحلُّ `divertConsoleToStderr()` من `tests/helpers/reportChannel.mjs` — وحارسُه مُشتقٌّ يقع من نفسه.

## 🔒 حدودٌ أمنيّة غيرُ قابلة للتفاوض

- **لا تُطبَع قيمةُ مفتاحٍ ولا جزءٌ منها** في أيّ تشخيص — الطولُ والشكلُ فقط.
- **المفاتيحُ تُضبَط في Render**، لا تُكتَب في المستودع ولا تُطلَب في المحادثة.
- **لا تُطلَب `MONGO_URI` ولا أيُّ سلسلةِ اتّصالٍ من المالك** — لا يحتاجها أحدٌ هنا.
- **نصُّ المزوّد الخام لا يُعرَض لصاحب المشروع** (وصل رابطُ فوترةِ حسابِ المنصّة إلى سجلّ مستخدمٍ مرّة). التشخيصُ بلفظِنا: `describeAIFailure`.

## أين تقرأ (١.٦ م.ب — لا تقرأها كلَّها)

| الملفّ | متى |
|---|---|
| `backend/ARCHITECTURE_MAP.md` | **ابدأ هنا** — كلُّ ملفٍّ وسطورُه ودورُه وحكمُه (KEEP/MODIFY/DELETE) |
| `backend/PRODUCT_MIND.md` | «عقلُ المنتج»: لماذا خرج تطبيقُ مطعمٍ لطلب تاكسي، وسلسلةُ PM/١..٢٥ |
| `backend/CONTRACTS.md` | العقودُ وسجلُّ كلِّ جولةٍ بأرقامها — **يُبحَث فيه، لا يُقرأ تسلسلاً** (١١٥٠٠ سطر) |
| `backend/ARCHITECTURE_MIGRATION.md` | لماذا اتُّخذ كلُّ قرارٍ معماريّ |
| `travel-service/CLAUDE.md` | حين يكون العملُ في جترافا |

**التسلسلُ الزمنيّ في `CONTRACTS.md` مقصود**: كلُّ قسمٍ يقول ما قِيس ومتى وما لم يُصلَح. لا تُعِد كتابتَه ولا تُلخّصه في ملفٍّ ثالث — يصير مصدرَ حقيقةٍ ثانياً يفترق عن الأوّل.

⚠️ الخرائطُ في الجذر (`*_ROADMAP.md`, `COMPETITIVE_ANALYSIS.md`) من **٢٠٢٦-٠٨-١٤** ولم تُراجَع منذُها — تُعامَل نيّةً قديمة لا وصفاً للحاضر.

## بنودٌ مفتوحةٌ مقيسةٌ لم تُبنَ (قرارُ المالك أو دليلٌ حيّ)

- **بوّابةُ الفهم — عتبةُ النسبة**: قِيست وسقطت. الصادقُ عند ٠٪ و١٧٪ والمهلوَسُ عند ١٣٪ — محشورٌ بينهما. العلّةُ **تغطيةُ المعجم لا العتبة**.
- **البوّابةُ تقع على احتياطٍ صادق**: محورُ «توافق الفئة مع نوع الطلب» نجح ٦/٦، ولم يُبنَ لأنّه **الاتّجاه المُهدِّئ** لحارس، وأدلّتُه فِخاخٌ مصنوعة لا سجلٌّ حيّ.
- **بنيةُ سلسلة المولّد**: أوّلُ خطوةٍ في حلقة `coderAgent` هي السلسلةُ كلُّها، ثمّ خطوتان على عميلَين استُنفدا. **قرارُ بنيةٍ لا رقعة**.
- **تشغيلُ شفرةٍ من مستودعٍ غريب**: `landRepoFiles` يُنزل ولا يُشغّل. **قرارٌ أمنيٌّ للمالك وحدَه.**

## التعامل مع المالك

يكتب بالعربيّة ويُرسل **سجلّات تشغيلٍ حيّة** — وهي أثمنُ مصدرِ أعطابٍ في هذه الشجرة: ثلاثةُ أعطابٍ حقيقيّة خرجت من سجلٍّ واحد. اقرأ السجلَّ سطراً سطراً وابحث عن **التناقض** (شيءٌ يُعلَن وشيءٌ يُفعَل) وعن **الصمت** (مقياسٌ لا يقول شيئاً فيُقرأ «لا مشكلة»).

القرارُ له وحدَه في: الدمج، وتشغيلُ شفرةٍ غريبة، وأيُّ تهدئةٍ لحارس.
