# 🤖 خارطة طريق: جولا بوت كمنتج مستقل (Chat Widget SaaS)

> الهدف النهائي: تحويل «جولا بوت» من **ميزة تنسخ ملفات داخل مشروع jaola** إلى
> **منتج مستقل يشترك فيه أي صاحب موقع** ويلصقه بسطر واحد — على غرار
> Intercom / Crisp / Tidio / Chatbase.

---

## 1. أين نحن الآن (مبنيّ ومدموج)

| اللبنة | الحالة | أين |
|---|---|---|
| ودجت مكتفٍ ذاتيّاً (زر عائم + لوحة دردشة) | ✅ | `backend/agents/jaolaBot.js` |
| White-label (يرث اسم/لون/رمز الموقع) | ✅ | `buildConfig` + `generateWidgetCSS` |
| قاعدة معرفة offline (قواعد + FAQ عربي + faq مخصّص) | ✅ | `defaultKB` + الودجت |
| ذكاء اصطناعي حيّ مع ارتداد آمن للقاعدة | ✅ | `POST /api/jaola-bot/chat` |
| توكن موقّع (HMAC) لهوية المشروع بلا تسريب مفاتيح | ✅ | `backend/agents/jaolaBotToken.js` |
| محدّد معدّل + سقف طول/مخرجات + CORS مخصّص | ✅ | `server.js` |
| إضافة بضغطة زرّ من لوحة التحكّم | ✅ | `Dashboard.jsx` |
| استوديو تخصيص للمستخدم (اسم/رمز/ترحيب/FAQ/أزرار سريعة/ذكاء) | ✅ | `Dashboard.jsx` (`botModal`) + `GET /api/jaola-bot/status` |
| بيان إعداد `.jaola-bot.json` (بلا أسرار) لملء الاستوديو بالقيم الحالية | ✅ | `jaolaBot.js` (`readBotManifest`) |
| مقتطف مُستضاف لمشاريع jaola (`embed.js?id=<توكن>`) | ✅ | `GET /api/jaola-bot/embed.js` |
| مستأجرون مستقلّون خارج jaola (سجلّ Mongo + إدارة داشبورد) | ✅ | `models/BotTenant.js` + `POST/GET/PUT/DELETE /api/bot-tenants[/:id]` + `GET /api/jaola-bot/tenant-embed.js` |

**الحقيقة الأساسية (محدَّثة 2026-07-31):** التوزيع والتخزين — العائق الذي كان
موثّقاً أعلاه — **حُلّ فعلاً**: عميل من خارج jaola يمكنه الآن الاشتراك مباشرة
(بلا مشروع jaola) وأخذ سطر تضمين مستقلّ. المتبقّي فعلياً هو طبقة الأعمال فوق
هذه البنية التقنية: الحصص والفوترة لكل مستأجر (قسم 3) — قرار تسعير، لا برمجة.

---

## 2. اللبنتان التاليتان (الأولوية — طلب المستخدم تسجيلهما)

### 2.1 المقتطف المُستضاف الواحد (Single Hosted Snippet) — ✅ أُنجز 2026-07-25
> نُفّذ عبر `GET /api/jaola-bot/embed.js?id=<توكن موقّع>` (حزمة JS واحدة تحقن
> CSS بنفسها، تُبنى من بيان `.jaola-bot.json`، بكاش 5 دقائق) + حقل «كود
> التضمين» بنسخ بضغطة في استوديو البوت. الهوية بالتوكن الموقّع الحالي —
> سجلّ المستأجرين المستقل (2.2) ✅ أُنجز أيضاً 2026-07-31 للعملاء من خارج jaola.
بدل نسخ `jaola-bot.js` + `jaola-bot.css` في كل مشروع، يلصق صاحب **أي موقع**
سطراً واحداً:

```html
<script src="https://bot.jaola.com/embed.js?id=ABC123"></script>
```

- الكود يبقى على خادمنا → **تحديث واحد يحسّن كل البوتات فوراً**.
- يعمل على أي موقع (WordPress / Shopify / مخصّص)، لا مواقع jaola فقط.
- العميل لا يتعامل مع ملفات — سطر واحد.

**التنفيذ المقترح:** نقطة `GET /embed.js?id=…` تُقدّم نفس منطق الودجت الحالي
(نعيد استخدام `WIDGET_BODY`)، لكنها تحقن الإعداد المجلوب من سجلّ المستأجر بدل
الحفر الثابت. + نقطة `GET /api/jaola-bot/config?id=…` تُرجع إعداد المستأجر.

### 2.2 تعدّد المستأجرين (Multi-tenancy) — ✅ أُنجز 2026-07-31
`id=<tenantId>` = مفتاح العميل. **سجلّ المستأجرين** (`models/BotTenant.js`، Mongo
collection) قائم فعلاً — لكل عميل إعداده المستقلّ تماماً:

```
a1b2c3...  → { ownerUsername, brandName, emoji, color, welcome, faq[], quick[], apiEnabled, createdAt }
d4e5f6...  → { … عميل مستقل تماماً }
```

- `GET /api/jaola-bot/tenant-embed.js?id=<tenantId>` يجلب إعداد هذا العميل وحده
  ويبني الحزمة عبر `buildEmbedBundle` نفسها المستخدمة لمشاريع jaola.
- إدارة الحساب: `POST/GET/PUT/DELETE /api/bot-tenants[/:id]` (`verifyToken` فقط،
  **بلا مشروع jaola مطلوب إطلاقاً**) + واجهة داشبورد «🌍 مساعد دردشة لأي موقع»
  (إنشاء/نسخ سطر التضمين/حذف).
- نقطة الدردشة الحيّة (`POST /api/jaola-bot/chat`) تُبدّل مصدر الهوية: تحاول
  التوكن الموقّع أولاً (مسار مشاريع jaola — **بلا أي تغيير سلوكي**، توافق خلفي
  كامل)، وإلا تبحث عن `token` كمعرّف مستأجر خام في السجلّ. حصّة الذكاء الشهرية
  نفسها (`botAiQuota`) تُطبَّق على حساب مالك المستأجر أيضاً.
- **متطلّب Mongo دائماً** (لا مسار offline بديل — سجلّ متعدّد مستأجرين حقيقي)؛
  عند غياب الاتصال: الإنشاء/التعديل/الحذف تعيد 503 صريحاً، والقائمة تعود فارغة
  بأمان بلا انهيار.
- **لبنة صريحة متبقّية للمنتج الكامل**: الحصص والفوترة لكل مستأجر (مذكورة في
  القسم 3 أدناه) — هذا قرار تسعير يخصّ المالك، لم يُبنَ بعد عمداً.

> 🛡️ ملاحظة أمان: مفاتيح المشاريع (MONGODB_URI…) تبقى مشفّرة ولا تُرسل للواجهة
> أبداً؛ نفس المبدأ يسري على أي سرّ في سجلّ المستأجرين. معرّف المستأجر (20 حرف
> hex عشوائي) بلا توقيع — نفس نموذج ثقة التوكن الحالي: ليس سرّاً، يمنع الانتحال
> لا الإخفاء، بمحدّدات معدّل صارمة على نقطة الدردشة.

---

## 3. لبنات لاحقة (بعد الأساس)

| اللبنة | الغاية |
|---|---|
| تخزين المحادثات + تحليلات | لوحة متابعة لكل عميل (أكثر الأسئلة، معدّل الحلّ) |
| ابتلاع المعرفة (رفع مستند/رابط → FAQ تلقائي) | تدريب البوت دون كتابة يدوية |
| تحويل لموظّف بشري (Inbox / Human handoff) | حين يعجز البوت |
| حصص وفوترة لكل عميل | ضبط تكلفة الذكاء + الطبقات (مجاني/Pro/Agency) |
| White-label للوكالات | إعادة بيع البوت باسم الوكيل |

---

## 4. نماذج التسويق المحتملة

1. **إضافة مدفوعة داخل jaola** (الأسهل): مجاني بالقواعد، ذكاء حيّ باشتراك شهري.
2. **SaaS مستقل «Jaola Bot»**: مقتطف واحد لأي موقع + لوحة إعداد + تسعير بالطبقات.
3. **إعادة بيع (Reseller/White-label)**: الوكالات ترخّصه وتبيعه لعملائها.

---

## 5. أول شريحة عمليّة موصى بها — ✅ منجزة (2026-07-31)

نقطة `GET /embed.js?id=…` تُقدّم الودجت + سجلّ مستأجرين بسيط (config لكل `id`)،
**دون لمس ما يعمل الآن** (النسخ الحالي داخل مشاريع jaola بقي مساراً موازياً
بلا أي تغيير سلوكي). ثبت النموذج من الطرف للطرف — القسم 2.2 أعلاه يوثّق التفاصيل.
**الخطوة التالية الحقيقية الآن**: أول لبنة من القسم 3 (الأقرب ثمرة: الحصص
والفوترة لكل مستأجر، أو تخزين المحادثات) — تحتاج قرار تسعير/أولوية من المالك
قبل البناء.

---

_سُجّلت هذه الخارطة بناءً على طلب المستخدم كنقطة مهمة يُعاد إليها لاحقاً._
